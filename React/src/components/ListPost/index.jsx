import React from "react"
import post from "./Post/post.js"

export default function Post() {
    return (
        <Container>
            {post.map(item => (
                <Post 
                    
                />
            ))}
        </Container>
    )
}